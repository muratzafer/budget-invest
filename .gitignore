# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*


pnpm-lock 2.yaml
pnpm-lock*.yaml~*


# env files (can opt-in for committing if needed)
.env
.env.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# ---- General ----
.DS_Store
*.pem
*.swp
*.swo
*.tmp
*.log

# ---- Node / Package managers ----
node_modules/
.pnp
.pnp.*

# Yarn v2+ folders (keep only patch/plugin/release metadata if used)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# pnpm / npm debug logs
pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Do NOT ignore the real lockfile; only temp/backup variants
# (uncomment next line if you explicitly don't want to commit lockfiles)
# pnpm-lock.yaml
pnpm-lock.*.backup
pnpm-lock.yaml.*
package-lock.*.backup
yarn.lock.*

# ---- Builds / Outputs ----
.next/
out/
dist/
build/
.vercel/
.turbo/

# ---- Tests / Coverage ----
coverage/

# ---- Environment files ----
.env
.env.local
.env.*.local

# ---- TypeScript / Next.js generated ----
*.tsbuildinfo
# (Optional) Next recommends committing this, but many teams ignore it:
next-env.d.ts

# ---- IDE / Editor ----
.vscode/
.idea/

# ---- Prisma / SQLite ----
prisma/dev.db
prisma/*.db*
prisma/migrations/*/migration.sql-journal

# ---- Generated code (adjust if you actually generate here) ----
/src/generated/prisma